# DSProd global configuration: defaults for all runs, committed to the repository.
#
# User-specific settings are NOT here — they go into config/user_custom.yaml, which is
# git-ignored and merged on top of this file (scalars and lists override; nested maps are
# deep-merged). See dsprod/config.py and docs/configuration/settings.md.
#
# `fs_default` is the single file system all products are written to, whatever the workflow
# backend (local / htcondor / crab). A plain /... path selects a local file system instead.
# The default below is the shared production area at FNAL (the LPC group area FLAF also uses);
# override it in user_custom.yaml to write to your own space, e.g.
#   fs_default: davs://eoshome-k.cern.ch:8444/eos/user/k/kandroso/DSProd/
#
# NOTE: xrootd, not davs — davs writes to cmseos.fnal.gov fail from CERN (dCache pool-redirect
# DNS), while xrootd reads and writes both work. Same choice as FLAF's fs_anaCacheTuple.
fs_default: root://cmseos.fnal.gov//eos/uscms/store/group/lpcflaf/DSProd/

# `fs_watchdog` (optional) holds the watchdog's heartbeat flags; unset = `fs_default`. Worth
# pointing elsewhere: the flags are one small overwrite per job per interval on a single flat
# directory, a metadata load quite unlike the products' few large writes, and a heartbeat says
# most about a job when the endpoint it is written to is not the one the products go to. e.g.
#   fs_watchdog: davs://eoshome-k.cern.ch:8444/eos/user/k/kandroso/DSProd_watchdog/

# CRAB backend: compute settings only. CRAB never stages out (DSProd owns all I/O), so there is
# no CRAB output location to configure.
#
# DSProd jobs have no real input dataset, so they may run at ANY CMS processing site. The CRAB
# client requires a whitelist whenever `ignoreLocality` is set, so an unset one defaults to every
# tier (T1_*, T2_*, T3_*) — the widest pool it accepts; setting one can only narrow it. CMS's
# global blacklist of known-broken sites stays in force.
# Optional keys:
#   whitelist: [ T2_CH_CERN, ... ]  restrict to these processing sites
#   blacklist: [ ... ]              exclude sites (e.g. ones that cannot reach your storage)
#   ignore_global_blacklist: true   waive CMS's known-broken-site list (not recommended)
#   parallel_jobs: 5000             jobs per CRAB task and in flight (--parallel-jobs wins)
#   refill_fraction: 0.2            min wave size / free slots, as a fraction of parallel_jobs
#   retry_release_minutes: 45       a retry parked by the wave gate goes out after this long,
#                                   whatever the wave size (0 = on the next poll)
#   auto_blacklist:                 quarantine sites whose recent jobs mostly fail (on by default;
#     enabled: true                 set `auto_blacklist: false` to keep only the static blacklist)
#     min_failures: 5               failures needed before a site can be quarantined at all
#     min_failure_rate: 0.5         ... and the fraction of jobs SENT there (ended + in flight)
#     relative_factor: 2.0          ... and how much worse than the other sites it must be
#     min_baseline_jobs: 20         ... judged against at least this many jobs elsewhere
#     quarantine_hours: 6           how long it stays out; afterwards its record starts clean
#     window_hours: 24              outcomes older than this stop counting
#     max_sites: 10                 never quarantine more than this many sites at once
#   watchdog:                       fail a job whose slot is held but whose payload has stopped.
#     enabled: true                 CRAB only -- `watchdog: false` switches it off entirely
#     interval_minutes: 30          how often a job refreshes its flag, and the driver lists them
#     missed_checks: 2              refreshes a job may miss before it is declared failed
#                                   (so the default declares a job dead after 60 min of silence)
#     max_per_interval: 5           brake: never fail more than this many jobs in one interval
#     max_per_branch: 1             how often one branch may be rescued this way; a branch that
#                                   stalls wherever it runs is the branch's problem, and every
#                                   verdict spends one of its four attempts
#     max_stale_fraction: 0.5       above this share of running jobs looking stale, read it as a
#                                   storage fault and issue nothing
#     dry_run: false                log the verdicts it would issue, issue none
crab:
  # Ceiling on the cores a CRAB job may request. CRAB accepts only 1, 2, 4 or 8 cores, and sells
  # at most 2500 MB per core -- 3000 MB for a single-core job. This caps both a task's own
  # `n_cpus` and the cores its `memory` request needs, so a value below what a task asks for makes
  # that task refuse to submit rather than silently under-request (a shrunk request is a kill
  # threshold, not a smaller reservation). Per-job memory is set on the task itself, next to
  # `n_cpus`; `memory_mb` below is only a default for tasks that name none.
  max_cores: 4
  # memory_mb: 0        # per-job request in MB for tasks with no `memory`; 0 = max(3000, 2500 * n)
  # mb_per_core: 2500   # CRAB's own limits; override only if CMS changes them
  # mb_single_core: 3000
